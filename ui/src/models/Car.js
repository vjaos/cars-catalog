export default class Car {
    constructor(number, color, brand, releaseYear) {
        this.number = number
        this.color = color
        this.brand = brand
        this.releaseYear = releaseYear
    }
}