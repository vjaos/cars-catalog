spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:db
    username: test
    password: test123

  jpa:
    hibernate:
      ddl-auto: create

  liquibase:
    enabled: false